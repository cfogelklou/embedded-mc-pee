/**
 * Gemini transport — lands in a later work package.
 *
 * This subpath export will provide the concrete transport implementation for `@google/genai`,
 * including schema-in-prompt rendering, degeneration detection, and record/replay support.
 *
 * @packageDocumentation
 */

// Re-exports will be added in WP-B:
// export { geminiTransport } from './transport';
// export type { GeminiTransportConfig } from './transport';
